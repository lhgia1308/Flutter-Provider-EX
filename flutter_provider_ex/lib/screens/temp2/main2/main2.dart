import 'package:flutter/material.dart';

class MainScreen2 extends StatelessWidget {
  MainScreen2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Main Screen 2"),
    );
  }
}
