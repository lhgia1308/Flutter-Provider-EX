import 'package:flutter/material.dart';

class DetailScreen6 extends StatelessWidget {
  DetailScreen6({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      decoration: BoxDecoration(
        color: Colors.amber,
      ),
      child: Text("Detail Screen 6"),
    );
  }
}
